// Build a function that takes in an array as a parameter. Return an object where the key is the element and the value is the number of occurrences for the the element that occurs the most often in the array. If there are ties return an object with all the highest occuring elements. If the paramenter passed in is not an array return null

const arr = [1, 4, 5, 3, 6, 7, 7, 4, 7, 7];

const mode = () => {};

mode(arr); // returns { 7: 4 }
