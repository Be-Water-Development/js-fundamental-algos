// Create an array that has every letter in the alphabet (hint do not hardcode-- attempt this programatically). From this array, return a new array that contains only vowels. For bonus points use the function `filter`

const letters = []; //create

const v = () => {};

v(letters); // ['A', 'E', 'I', 'O', 'U', 'Y']
